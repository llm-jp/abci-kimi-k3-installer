# Kimi K3 on ABCI 検証メモ

この文書は、本インストーラを作成するまでに得られた、運用・保守・再検証に有用な知見をまとめたものです。時系列の作業履歴、ジョブID、利用者固有のパス、一時的な検証スクリプトの詳細は含めていません。

## 検証済みの構成

2026年7月に、ABCIのNVIDIA H200搭載ノードで次の構成を検証しました。

- 1レプリカ: 2ノード、H200×16
- ノードあたり: H200×8
- SGLang: commit `9cb03516b2baa9b42a418de98deea491a9ab8eb9`
- PyTorch: `2.11.0+cu130`
- 並列化: TP16、EP16
- MoE backend: Marlin
- decode attention backend: FlashMLA
- symmetric memory: 有効
- `mem-fraction-static`: `0.85`
- `mamba-full-memory-ratio`: `0.45`
- ノード間通信: `ibn1`上のNCCL/IB
- 複数レプリカの集約: SGLang Router `0.3.2`、`cache_aware`方式

1、2、4レプリカをそれぞれ起動し、最大8ノード、H200×64で以下を確認しました。

- 全レプリカのモデル読込みとCUDAグラフ生成
- OpenAI互換の`/v1/models`と`/v1/chat/completions`
- Kimi K3のreasoning内容と最終回答の分離
- Routerによる単一エンドポイント化
- 4レプリカへの並列リクエスト分散

4レプリカへ16件のリクエストを同時送信した試験では、各レプリカに4件ずつ振り分けられました。

## ネイティブ構成を採用した理由

Singularity内でもGPUとNCCL collectiveは動作しましたが、RDMA HCAを開けず、NCCLがIB以外の経路へフォールバックしました。ホスト上では同じRDMAデバイスを利用できたため、本インストーラではコンテナを使用せず、共有ストレージ上のPython仮想環境から直接実行します。

ネイティブ構成の2ノード16 GPU試験では、全rankでNCCLのIB transport利用を確認しました。256 MiBのBF16 all-reduceで得た参考値は次のとおりです。

```text
world size:    16
average:       1.56 ms
algorithm BW: 172.33 GB/s
bus BW:       323.11 GB/s
```

NCCLの判定では、collectiveの完走だけでなく、全rankのログでIBデバイスと`Using network IB`を確認し、`ibv_open_device failed`、`No device found`、Socketへのフォールバック、`NCCL WARN`がないことを確認する必要があります。

## 起動時に必要な対策

### Transformersキャッシュの事前生成

Kimi K3は`trust_remote_code`で独自のtokenizer/processorコードを読み込みます。空のTransformers dynamic-module cacheを同一ノード上の複数プロセスから同時生成すると、ファイル配置の競合により`ModuleNotFoundError`が発生することがあります。

本インストーラはジョブ・ノード別のキャッシュを作成し、各ノードで単一プロセスによる事前読込みを完了してからSGLangを起動します。この処理は削除しないでください。

### NCCLのリンク用ライブラリ

SGLangはCUDAグラフの初期化中に`nccl_allocator`拡張をJITビルドし、リンク時に非バージョン付きの`libnccl.so`を必要とします。

ABCIの`nccl/2.28/2.28.3-1`モジュールは、このリンク用ライブラリとヘッダを提供します。一方、PyTorch本体は自身のRPATHによりwheel同梱の`libnccl.so.2`を使用します。この組合せを実機で確認しているため、独自のNCCLシンボリックリンクは不要です。

`cannot find -lnccl`が発生した場合は、重みやGPUメモリを調整する前に、NCCLモジュールの読込みと次の解決結果を確認してください。

```bash
c++ -print-file-name=libnccl.so
```

### ノードとGPUの割当て

- PBSのノードスロット順に2ノードずつ組にし、各組を独立したレプリカとして起動します。
- MPIはレプリカごとのhostfileと`-np 2`を使用します。ABCIで使用したOpen MPIでは`--map-by`が受理されなかったため使用しません。
- PBSが`CUDA_VISIBLE_DEVICES`に単一GPU UUIDを設定する場合があるため、各ノードの8 GPUを確認した後にこの変数を解除します。
- 分散初期化、NCCL、Glooには、ホスト名ではなく`ibn1`のIPv4アドレスを使用します。

## 接続経路

ログインノードから計算ノードの任意ポートへ直接接続できるとは限りません。サーバーがreadyでPBSジョブも実行中なのに接続を拒否された場合は、サーバー障害と決めつけず、まず接続経路を確認してください。

本インストーラはRouterを先頭レプリカのleaderノード上の`31000`番ポートで起動します。利用時はREADMEに記載したCPUジョブを投入し、そのジョブ内でRouterノードへのSSHポートフォワーディングを作成します。Routerを外部へ直接公開する構成ではありません。

## 起動時間の目安

モデルの重みの読込みとCUDAグラフ生成を含む初回試験では、readyになるまで約10～14分かかりました。2レプリカは約11分、4レプリカは約11分で全レプリカがreadyになっています。

ストレージ負荷、キャッシュ状態、同時実行状況で変動するため、固定の保証値ではありません。起動直後の接続拒否だけで失敗と判定せず、PBSジョブの状態とノード別ログを確認してください。

## トークン容量

検証時の値は次のとおりです。

```text
モデル定義上のcontext window: 1,048,576 tokens
KV token slots:                 396,416
実効最大入力長:                396,410 tokens
```

実効上限はモデル定義ではなく、`mem-fraction-static=0.85`で確保されたKV poolに制約されました。396,411 tokensの入力がHTTP 400で拒否され、その後も全workerがhealthyであることを確認しています。

この値はSGLang、依存パッケージ、起動引数、GPUメモリ利用状況によって変わり得ます。インストーラの更新後は、READMEの`client.sh capacity`で実環境の値を再確認してください。

## 性能の参考値

次の固定条件で、1～4レプリカのスケーリングを測定しました。

- 入力: 1,024 tokens/request
- 出力: 256 tokens/request
- 並列度: 1レプリカあたり34
- 各条件の前に同じ並列度でwarmupを1回実施
- 測定: 3 waves
- Routerの影響を除き、対象workerへ直接かつ均等に送信

| レプリカ | H200 | 出力 tok/s | 入力 tok/s | 合計 tok/s | req/s | 平均遅延 | p95遅延 |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 16 | 524.084 | 2,096.335 | 2,620.419 | 2.047 | 16.598 s | 16.794 s |
| 2 | 32 | 1,022.342 | 4,089.368 | 5,111.710 | 3.994 | 16.947 s | 17.342 s |
| 3 | 48 | 1,542.197 | 6,168.790 | 7,710.987 | 6.024 | 16.836 s | 17.095 s |
| 4 | 64 | 2,018.557 | 8,074.226 | 10,092.783 | 7.885 | 16.978 s | 17.734 s |

4レプリカ時の出力スループットは1レプリカ比3.8516倍、スケーリング効率は96.29%でした。1レプリカでは並列度34が測定範囲内の最大スループットを示し、並列度48、64では改善せず遅延が増加しました。

これらは容量計画の初期値としてのみ使用してください。性能比較では入力長、出力長、並列度、warmup、送信先をそろえ、READMEのbenchmarkコマンドで再測定してください。

## 検証範囲と更新時の注意

- 1～4レプリカの起動、Router、チャット生成、分散、容量、性能は、上記の固定commitと実行条件で実機検証済みです。
- 現在の配布物は成功した構成を整理したものですが、その後の簡素化を含む全ファイルを使ったサーバーのend-to-end再実行までは行っていません。変更時はまず1レプリカで起動・チャット生成を確認してから増設してください。
- tool-call parserは設定済みですが、tool callの一連の応答は未検証です。
- 画像入力は未検証です。
- インストールはGitHub、PyPI、PyTorch、Rustup、crates.ioへの接続を必要とします。
- `RUNTIME_ROOT`は新規パス専用です。既存ディレクトリへの再インストールや途中再開は行いません。
- レプリカ数にスクリプト上の上限はありませんが、実際の上限はABCI Spotサービスの利用可能資源で決まります。

構成や依存バージョンを変更した場合は、少なくともモデルファイル検査、Python/Rust extensionのimport、1レプリカの起動、NCCL/IB、OpenAI互換API、実効トークン上限を順に再確認してください。
